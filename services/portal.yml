name: portal
env:
  - CART_MODE=release
  - GO_ENV=production

command:
  - ./home/portal/portal

pid_file: ./home/portal/portal.pid
grace: true
run_at_load: true
keep_alive: false

deployment:
  type: gitlab
  repository: https://gitlab.com/gimke/portalbuild
  version: darwin
  payload: http://localhost:8080/home