name: cartdemo
env:
  - CART_MODE=release

command:
  - ./home/cartdemo/cartdemo

pid_file: ./home/cartdemo/cartdemo.pid
grace: true
run_at_load: false
keep_alive: false

#deployment:
#  type: github (only support github gitlab)
#  token: Personal access tokens (visit https://github.com/settings/tokens and generate a new token)
#  repository: repository address (https://github.com/gimke/cartdemo)
#  version: release:latest, release:v1.0.3, branch:master or branch:other_branch
#  payload: payload url when update success

#  type: gitlab (only support github gitlab)
#  token: Personal access tokens (visit https://gitlab.com/profile/personal_access_tokens and generate a new token)
#  repository: repository address (https://gitlab.com/gimke/cartdemo)
#  version: release:latest, release:v1.0.3, branch:master or branch:other_branch
#  payload: payload url when update success

deploy:
  type: github
  repository: https://github.com/gimke/cartdemo
  version: master
  payload: http://localhost:8080/payload